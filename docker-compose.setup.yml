services:
  setup:
    container_name: pacs-setup
    build:
      context: .
      dockerfile: setup/Dockerfile
    ports:
      - 3001:3001
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - .:/project
    restart: "no"
